.container {
  height: 100vh;
  background-color: var(--bg-color);
  display: flex; flex-direction: column;
}

/* 顶部 */
.header-section {
  background: var(--card-bg);
  padding: 20rpx 30rpx 0;
  box-shadow: 0 4rpx 10rpx rgba(141, 110, 99, 0.05);
  z-index: 10;
}

.search-bar {
  background: #F5F5F5;
  border-radius: 40rpx;
  padding: 16rpx 30rpx;
  display: flex; align-items: center;
  margin-bottom: 20rpx;
}
.search-icon { margin-right: 10rpx; color: #999; }
.search-input { font-size: 26rpx; width: 100%; }
.ph-style { color: #bbb; }

.tab-row { display: flex; align-items: center; }
.tab-item {
  margin-right: 40rpx; padding: 20rpx 0;
  font-size: 30rpx; color: var(--text-sub);
  position: relative; font-weight: 500;
}
.tab-item.active { color: #FF7043; font-weight: 900; font-size: 32rpx; }
.tab-line {
  position: absolute; bottom: 0; left: 50%; transform: translateX(-50%);
  width: 40rpx; height: 6rpx; background: #FF7043; border-radius: 4rpx;
}

/* 列表 */
.post-list { flex: 1; padding: 20rpx; box-sizing: border-box; }

.post-card {
  background: var(--card-bg);
  border-radius: 24rpx;
  padding: 30rpx; margin-bottom: 24rpx;
  box-shadow: 0 4rpx 12rpx rgba(141, 110, 99, 0.08);
}

.card-header { display: flex; align-items: center; margin-bottom: 20rpx; }
.avatar { width: 80rpx; height: 80rpx; border-radius: 50%; margin-right: 20rpx; background: #eee; }
.user-meta { flex: 1; display: flex; flex-direction: column; }
.nickname { font-size: 30rpx; font-weight: bold; color: var(--text-main); }
.time { font-size: 22rpx; color: var(--text-sub); margin-top: 4rpx; }
.bounty-tag {
  background: #FFF3E0; color: #FF7043; font-size: 24rpx; 
  padding: 4rpx 16rpx; border-radius: 20rpx; font-weight: bold;
  border: 1rpx solid #FFCCBC;
}

.card-body { margin-bottom: 20rpx; }
.post-title { font-size: 32rpx; font-weight: 900; color: var(--text-main); margin-bottom: 10rpx; }
.post-content { font-size: 28rpx; color: #5D4037; line-height: 1.6; margin-bottom: 16rpx; }
.img-grid { display: flex; flex-wrap: wrap; gap: 10rpx; }
.grid-img { width: 200rpx; height: 200rpx; border-radius: 12rpx; background: #f0f0f0; }

.card-footer { display: flex; justify-content: space-between; align-items: center; }
.topic-tag { font-size: 24rpx; color: #1E88E5; background: #E3F2FD; padding: 4rpx 12rpx; border-radius: 8rpx; }
.action-row { display: flex; gap: 40rpx; }
.action-btn { font-size: 26rpx; color: var(--text-sub); display: flex; align-items: center; }

/* 悬浮按钮 */
.fab-btn {
  position: fixed; bottom: 60rpx; right: 40rpx;
  width: 110rpx; height: 110rpx;
  background: linear-gradient(135deg, #FF7043, #D84315);
  border-radius: 50%;
  display: flex; flex-direction: column; align-items: center; justify-content: center;
  box-shadow: 0 10rpx 30rpx rgba(216, 67, 21, 0.4);
  color: white; z-index: 100;
}
.plus-icon { font-size: 40rpx; font-weight: bold; line-height: 1; }
.fab-text { font-size: 20rpx; font-weight: bold; margin-top: 4rpx; }

.loading-tips { text-align: center; color: #ccc; font-size: 24rpx; padding: 20rpx; }