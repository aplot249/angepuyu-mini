<view class="container font-lvl-{{fontSizeLevel}} {{isDarkMode ? 'dark-mode' : ''}}">
  
  <!-- 首页 Banner -->
  <view class="home-banner">
    <view class="greeting">Jambo! 👋</view>
    <view class="date-info">{{currentDate}}</view>
  </view>

  <!-- 每日推荐卡片 -->
  <view class="section-title">每日一句</view>
  
  <view class="daily-card-container">
    <!-- 如果是单词类型 (一行两列布局，无图) -->
    <block wx:if="{{dailyWord.type === 'word'}}">
      <view class="card item-card word-layout daily-style">
        <view class="card-main">
          <view class="info-col">
            <view class="row-flex">
              <text class="sw-text">{{dailyWord.swahili}}</text>
              <view class="play-btn" bindtap="playDailyAudio">🔊</view>
            </view>
            
            <view class="homonym-box">
              <text class="tag">谐音</text>
              <text class="hm-text">{{dailyWord.homonym}}</text>
            </view>

            <view class="trans-box">
              <text class="cn-text">{{dailyWord.chinese}}</text>
              <text class="en-text">{{dailyWord.english}}</text>
            </view>
          </view>
          <!-- 已移除图片 -->
        </view>
        
        <view class="card-footer">
          <view class="action-fav" bindtap="toggleDailyFav">
            <text class="fav-icon {{dailyWord.isFav ? 'active' : ''}}">{{dailyWord.isFav ? '❤️' : '🤍'}}</text>
            <text class="fav-label {{dailyWord.isFav ? 'active' : ''}}">收藏</text>
          </view>
          <view class="action-fix"><text>纠错</text></view>
        </view>
      </view>
    </block>

    <!-- 如果是短语类型 (跨两列布局，保持通栏) -->
    <block wx:else>
      <view class="card item-card phrase-layout daily-style">
        <view class="phrase-header">
          <text class="sw-sentence">{{dailyWord.swahili}}</text>
          <view class="play-btn large" bindtap="playDailyAudio">🔊</view>
        </view>
        <view class="homonym-row">
          <text class="tag">谐音</text>
          <text class="hm-sentence">{{dailyWord.homonym}}</text>
        </view>
        <view class="meaning-col">
          <text class="cn-sentence">{{dailyWord.chinese}}</text>
          <text class="en-sentence">{{dailyWord.english}}</text>
        </view>
        <view class="card-footer phrase-footer">
          <view class="action-fav" bindtap="toggleDailyFav">
            <text class="fav-icon {{dailyWord.isFav ? 'active' : ''}}">{{dailyWord.isFav ? '❤️' : '🤍'}}</text>
            <text class="fav-label {{dailyWord.isFav ? 'active' : ''}}">收藏</text>
          </view>
          <view class="action-fix"><text>纠错</text></view>
        </view>
      </view>
    </block>
  </view>

  <!-- 快捷入口 -->
  <view class="quick-nav">
    <view class="nav-item" bindtap="goCategory">
      <view class="nav-icon orange">🗂</view>
      <text>分类词库</text>
    </view>
    <view class="nav-item" bindtap="goWordbook">
      <view class="nav-icon purple">⭐</view>
      <text>我的收藏</text>
    </view>
  </view>

</view>