<view class="container font-lvl-{{fontSizeLevel}} {{isDarkMode ? 'dark-mode' : ''}}">
  <!-- 轮播图 -->
  <swiper class="banner-swiper" indicator-dots autoplay circular indicator-active-color="#fff">
    <block wx:for="{{banners}}" wx:key="*this">
      <swiper-item>
        <image src="{{item.image_url}}" mode="aspectFill" class="banner-img"></image>
        <view class="banner-text">
          <text class="tag">{{item.type}}</text>
          <text class="title">{{item.name}}</text>
        </view>
      </swiper-item>
    </block>
  </swiper>

  <!-- 滚动公告 -->
  <view class="notice-bar">
    <view class="icon">📢</view>
    <swiper class="notice-swiper" autoplay vertical circular>
      <block wx:for="{{notices}}" wx:key="*this">
        <swiper-item class="notice-item">{{item.content}}</swiper-item>
      </block>
    </swiper>
  </view>

  <!-- 每日单词 -->
  <view class="section-header">
    <view>
      <text class="h2">每日单词</text>
      <view class="desc">积少成多，每天进步一点点</view>
    </view>
    <view class="btn-refresh" bindtap="shuffleDaily">🔄 换一批</view>
  </view>

  <view class="grid-2">
    <block wx:for="{{dailyWords}}" wx:key="id">
      <view class="card word-card" bindtap="playAudio" data-item="{{item}}" data-type="word">
        <text class="swahili">{{item.swahili}}</text>
        <view class="homonym">🔊 {{item.homonym}}</view>
        <view class="chinese">{{item.chinese}}</view>
      </view>
    </block>
  </view>

  <!-- 实用短语 -->
  <view class="section-header">
    <view>
      <text class="h2">实用短语</text>
      <view class="desc">地道表达，沟通无障碍</view>
    </view>
  </view>

  <view class="phrase-list">
    <block wx:for="{{dailyPhrases}}" wx:key="id">
      <view class="card phrase-card" bindtap="playAudio" data-item="{{item}}" data-type="phrase">
        <view class="flex-col">
          <text class="swahili-lg">{{item.swahili}}</text>
          <text class="chinese-sm">{{item.chinese}}</text>
        </view>
        <view class="play-icon">🔊</view>
      </view>
    </block>
  </view>
</view>